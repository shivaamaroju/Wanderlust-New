apiVersion: v1
kind: Service
metadata:
  name: wanderlust-service
spec:
  type: LoadBalancer
  selector:
    app: wanderlust
  ports:
    - protocol: TCP
      port: 8082        
      targetPort: 8082 
